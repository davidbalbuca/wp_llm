# Secretos
.env

# Binarios compilados (solo en la raíz; NO ignorar el paquete cmd/bot)
/dist/
/bot
/bot.exe

# Logs
*.log

# Base de datos SQLite
/data/

# Copia del .env de PRODUCCION (secretos reales). NUNCA a git.
deploy/prod/
.env.prod
.env.*
